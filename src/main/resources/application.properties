# Kafka broker address
spring.kafka.bootstrap-servers=localhost:9092

# Consumer group ID
spring.kafka.consumer.group-id=garden-group

# Auto-reset consumer offset to the latest
spring.kafka.consumer.auto-offset-reset=latest
